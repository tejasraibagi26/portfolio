import React, {useState} from 'react';
import {Navbar, Nav, Col, Row, Button, Container,Toast} from 'react-bootstrap';
import LandingPage from './Landing';

function Main() {
        return(
            <LandingPage /> 
        );
}

export default Main;